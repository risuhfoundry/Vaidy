openai>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pymupdf>=1.24.0

# Local fallback extractors. They are optional at runtime; the CLI reports a
# clear fallback status when either package is not installed.
docling>=2.0.0
paddleocr>=2.7.0,<3.0.0
paddlepaddle==2.6.2
albumentations>=1.4.0,<2.0.0
